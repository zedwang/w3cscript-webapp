angular.module('w3cscript.services', [])

/**
 * A simple example service that returns some data.
 */
.factory('w3cServers', function() {
  // Might use a resource here that returns a JSON array

  // Some fake testing data

});
